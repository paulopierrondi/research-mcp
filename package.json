{
  "name": "research-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP Server for ChatGPT + Gemini research with history",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "openai": "latest",
    "@google/generative-ai": "latest",
    "zod": "latest"
  }
}
